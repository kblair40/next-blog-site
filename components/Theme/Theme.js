import React from "react";

import styles from "./Theme.module.scss";

const Theme = () => {
  return (
    <div className={styles.container}>
      <section>
        <h3 classname={styles.container__heading}>Fonts</h3>
      </section>
    </div>
  );
};

export default Theme;
